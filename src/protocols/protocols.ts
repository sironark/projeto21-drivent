export type Cep = {
  cep: string;
};

export type CepError = {
  error: boolean;
};

export type EnrollmentAddress = {
  logradouro: string;
  complemento: string;
  bairro: string;
  cidade: string;
  uf: string;
};
